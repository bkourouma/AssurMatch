/**
 * The fonts are declared once in `@assurmatch/ui/fonts` and re-exported here so the public app and
 * the two back-offices load exactly the same Nunito/Inter subsets behind --font-am-heading /
 * --font-am-body.
 */
export { bodyFont, headingFont } from "@assurmatch/ui/fonts";
