# Tasks: Partner Webhook Event Wiring

**Impacted surfaces**: Backend API.

- [x] T001 `PartnerWebhookEventPublisher` with fail-safe behaviour and audit.
- [x] T002 Publish `lead.assigned` and `lead.status_changed` from the lead and CRM services.
- [x] T003 Publish `notification.failed` from the notification failure path; runtime wiring.
- [x] T004 Runtime HTTP tests; `npm run validate`; tick tasks.
