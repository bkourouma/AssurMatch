# Tasks: Quote Form Definition Persistence and Administration

**Impacted surfaces**: Backend API, database / Prisma (read path only), shared packages, Back-office Plateforme.
**Status**: implemented and validated on 2026-09-19. No migration required: the table and every column already existed.

- [x] T000 D1 resolved against the existing role matrix instead of the proposal: `packages/shared/rbac/assurmatch-role-matrix.ts` already grants `quote_form_definitions:*` to `compliance_admin`, only `:read` to `content_admin` and nothing to `admin_pays`. The matrix is left untouched and the service checks those permissions, so there is no country-scope branch to write. D2 and D3 implemented as specified.
- [x] T001 `QuoteFormDefinitionsRepository` interface + `MemoryQuoteFormDefinitionsRepository` (test-only) + `PrismaQuoteFormDefinitionsRepository`, with `validationSchema` passthrough. Register in `runtimeRepositoryModes()` and guard with `assertRuntimeRepository`. Test: constructing the runtime with the memory repository outside `NODE_ENV=test` throws.
- [x] T002 Service takes the repository; `create`, `publish`, `retire`, `list`, `require` become async; update all call sites including `activation-checklist.service.ts` (`Awaited<ReturnType<...>>`). Test: a definition written directly to Postgres is returned by a process started afterwards.
- [x] T003 Publish invariants in order - consent published and `lead_transmission`, no forbidden public wording in labels, sensitive fields gated by `product_sensitive_data_enabled`, role and country scope, MFA - each refusal audited with its own reason. Unit test per invariant.
- [x] T004 Atomic version swap: publishing retires the previously published definition for the (country, product, language) triple in one transaction. Tests: exactly one published definition after a v2 publish; a failed transaction leaves v1 published and v2 not.
- [x] T005 HTTP wiring: `GET /admin/quote-form-definitions`, `POST /admin/quote-form-definitions`, `POST /:id/publish`, `POST /:id/retire`, each with an audited reason. Verify the status mapping produced by `error-response.filter.ts` for every refusal reason.
- [x] T006 Contracts: list query schema and admin read schema exposing `status`, `publishedAt`, `retiredAt` and the resolved consent text version.
- [x] T007 Admin screen replacing `apps/admin/app/quote-form-definitions/page.tsx`: list, create draft, publish, retire, with an explicit statement that publishing exposes the form publicly and binds the consent text version. Source tests for forbidden wording and for the publish confirmation copy.
- [x] T008 Close the blind spot: `runtime-postgres-smoke-seed.ts` seeds its form over HTTP through the admin route instead of `runtime.quoteForms.service.create(...)`; `scripts/local-app/local-health.mjs` asserts a rendered form field instead of the `Demander un devis` heading.
- [x] T009 Reconsidered and done differently: the preprod reference seed does **not** publish definitions. Publishing one requires a published consent text, whose content is a legal artifact drafted per country - generating it to make a checklist green would fabricate the very evidence the checklist exists to verify. `docs/runbooks/activation-product.md` gains the publication step instead, which is now executable because the routes exist.
- [x] T011 Found and fixed while running the journey: CORS was never enabled (documented `CORS_ORIGINS` never read), and the visitor's answers were dropped twice - by the public form, which ignored the definition's fields, and by the routing service, which did not carry them onto the assignment.
- [x] T010 `npm run validate`, `npx playwright test`, `npm run test:runtime:postgres:docker`; then `launch-local.bat` + demo seed + a real browser submission reaching the broker CRM with its answers. Tasks ticked; `docs/prd_coverage_map.md` updated, including the newly found notification-delivery gap as backlog item 0b.
